// npm --version

// to install package for project only
// npm i <packagename>

// to install package globally
// npm install -g <pcakagename>
// sudo install -g <packagename>

// package.json stores important information about the application/project/package
// 3 ways to create package.json file
// manual
// npm init (follow prompt on cmd)
// npm init -y (default create package.json)
